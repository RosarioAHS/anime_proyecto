{% extends 'base.html.twig' %}

{% block title %}Mis Rankings - Studio Ghibli{% endblock %}

{% block body %}
    <div class="container">
        <div class="row mb-4">
            <div class="col-md-8">
                <h1 class="text-white">
                    <i class="fas fa-list me-2"></i>Mis Rankings
                </h1>
                <p class="text-white-50">Tus listas personalizadas de películas</p>
            </div>
            <div class="col-md-4 text-end">
                <a href="{{ path('rankings_nuevo') }}" class="btn btn-success">
                    <i class="fas fa-plus me-2"></i>Crear nuevo ranking
                </a>
                <a href="{{ path('rankings_publicos') }}" class="btn btn-info">
                    <i class="fas fa-globe me-2"></i>Ver rankings públicos
                </a>
            </div>
        </div>

        {% if rankings is empty %}
            <div class="card shadow-lg">
                <div class="card-body text-center py-5">
                    <i class="fas fa-list-alt fa-4x text-muted mb-3"></i>
                    <h4>No has creado ningún ranking aún</h4>
                    <p class="text-muted">Crea listas personalizadas con tus películas favoritas</p>
                    <a href="{{ path('rankings_nuevo') }}" class="btn btn-success mt-3">
                        <i class="fas fa-plus me-2"></i>Crear mi primer ranking
                    </a>
                </div>
            </div>
        {% else %}
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
                {% for ranking in rankings %}
                    <div class="col">
                        <div class="card h-100 shadow-lg hover-card">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-start mb-3">
                                    <h5 class="card-title">{{ ranking.nombreRanking }}</h5>
                                    {% if ranking.publico %}
                                        <span class="badge bg-success">
                                        <i class="fas fa-globe me-1"></i>Público
                                    </span>
                                    {% else %}
                                        <span class="badge bg-secondary">
                                        <i class="fas fa-lock me-1"></i>Privado
                                    </span>
                                    {% endif %}
                                </div>

                                {% if ranking.descripcion %}
                                    <p class="card-text text-muted small">{{ ranking.descripcion }}</p>
                                {% endif %}

                                <div class="mb-3">
                                <span class="badge bg-primary">
                                    <i class="fas fa-film me-1"></i>{{ ranking.rankingPeliculas|length }} películas
                                </span>
                                    <small class="text-muted ms-2">
                                        <i class="fas fa-clock me-1"></i>{{ ranking.creadoEn|date('d/m/Y') }}
                                    </small>
                                </div>

                                <!-- Preview de películas -->
                                {% if ranking.rankingPeliculas|length > 0 %}
                                    <div class="d-flex gap-1 mb-3">
                                        {% for rp in ranking.rankingPeliculas|slice(0, 3) %}
                                            {% if rp.pelicula.imagenUrl %}
                                                <img src="{{ rp.pelicula.imagenUrl }}"
                                                     class="img-thumbnail"
                                                     style="width: 60px; height: 80px; object-fit: cover;"
                                                     title="{{ rp.pelicula.titulo }}">
                                            {% endif %}
                                        {% endfor %}
                                        {% if ranking.rankingPeliculas|length > 3 %}
                                            <div class="d-flex align-items-center justify-content-center bg-light rounded" style="width: 60px; height: 80px;">
                                                <span class="text-muted">+{{ ranking.rankingPeliculas|length - 3 }}</span>
                                            </div>
                                        {% endif %}
                                    </div>
                                {% endif %}

                                <div class="d-grid gap-2">
                                    <a href="{{ path('rankings_show', {'id': ranking.id}) }}" class="btn btn-primary btn-sm">
                                        <i class="fas fa-eye me-1"></i>Ver ranking
                                    </a>
                                    <div class="btn-group">
                                        <a href="{{ path('rankings_gestionar', {'id': ranking.id}) }}" class="btn btn-outline-secondary btn-sm">
                                            <i class="fas fa-cog me-1"></i>Gestionar
                                        </a>
                                        <a href="{{ path('rankings_editar', {'id': ranking.id}) }}" class="btn btn-outline-warning btn-sm">
                                            <i class="fas fa-edit me-1"></i>Editar
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>

    {% block stylesheets %}
        {{ parent() }}
        <style>
            .hover-card {
                transition: all 0.3s ease;
            }
            .hover-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 10px 30px rgba(0,0,0,0.3) !important;
            }
        </style>
    {% endblock %}
{% endblock %}
